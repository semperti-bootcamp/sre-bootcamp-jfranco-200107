---

    - name: Descarga de Imagen de Docker
      connection: network_cli
      gather_facts: false
      become: yes
      hosts: all
      tasks:
  
        - name: Cerramos containers abiertos
          shell: "docker stop $(docker ps -q)"
          ignore_errors: yes