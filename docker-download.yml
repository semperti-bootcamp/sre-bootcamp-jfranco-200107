---

    - name: Descarga de Imagen de Docker
      connection: network_cli
      gather_facts: false
      become: yes
      hosts: all
      vars:
          version: "{{ version }}"
      tasks:
  
        - name: Hacemos pull de la imagen
          shell: "docker pull jsfrnc/bootcamp-journals:{{version}}"
          